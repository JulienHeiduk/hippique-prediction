"""Lab: experimental models / features for offline comparison vs. production.

Nothing in this package touches production code paths. Variants are registered
in :mod:`src.lab.registry`; the walk-forward harness lives in
:mod:`src.lab.harness`.
"""
